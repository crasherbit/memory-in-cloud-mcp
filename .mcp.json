{
  "mcpServers": {
    "memory": {
      "url": "http://localhost:8080/mcp",
      "transport": "http",
      "headers": {
        "Authorization": "Bearer ${MCP_MEM_KEY}",
        "X-Agent-Id": "claude-code:${USER}"
      }
    }
  }
}
